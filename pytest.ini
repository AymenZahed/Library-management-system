[tool:pytest]
DJANGO_SETTINGS_MODULE = backend.settings
python_files = test_*.py *_tests.py
addopts = 
    --cov=backend
    --cov-report=html
    --cov-report=xml
    --cov-report=term-missing
    --verbose
    -ra
testpaths = tests