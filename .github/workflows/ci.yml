name: CI/CD Pipeline - Library Management System

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

env:
  PYTHON_VERSION: '3.10'

jobs:
  initiation:
    name: 🚀 Initialisation du Pipeline
    runs-on: ubuntu-latest
    steps:
      - name: 📢 Message de démarrage
        run: |
          echo "╔════════════════════════════════════╗"
          echo "║   🚀 DÉMARRAGE DU PIPELINE         ║"
          echo "║   📚 Projet: Library Management    ║"
          echo "║   👤 Déclenché par: ${{ github.actor }} ║"
          echo "║   🌿 Branche: ${{ github.ref_name }}    ║"
          echo "║   📋 Event: ${{ github.event_name }}    ║"
          echo "╚════════════════════════════════════╝"

  discover-services:
    name: 🔍 Découverte des Microservices
    runs-on: ubuntu-latest
    needs: initiation
    outputs:
      services: ${{ steps.find-services.outputs.services }}
    steps:
      - uses: actions/checkout@v4

      - name: 🔍 Trouver les services
        id: find-services
        run: |
          SERVICES=$(find backend -maxdepth 1 -type d -name "*_service" | sed 's|backend/||' | sort)
          if [ -z "$SERVICES" ]; then
            SERVICES="user_service"
          fi
          JSON=$(echo "$SERVICES" | jq -R -s -c 'split("\n") | map(select(length>0))')
          echo "services=$JSON" >> $GITHUB_OUTPUT
          echo "📦 Services trouvés: $SERVICES"

  tests-unitaires:
    name: 🧪 Tests Unitaires et Couverture
    runs-on: ubuntu-latest
    needs: discover-services
    strategy:
      matrix:
        service: ${{ fromJson(needs.discover-services.outputs.services) }}
      fail-fast: false
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: library_test
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h127.0.0.1 -proot"
          --health-interval=5s
          --health-timeout=3s
          --health-retries=10
    steps:
      - uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: 📦 Installer dépendances globales
        working-directory: ./backend
        run: |
          pip install --upgrade pip setuptools wheel
          pip install -r requirements.txt
          pip install mysqlclient djangorestframework-simplejwt pytest pytest-cov pytest-django

      - name: 📦 Installer dépendances du service
        working-directory: ./backend/${{ matrix.service }}
        run: |
          if [ -f "requirements.txt" ]; then
            pip install -r requirements.txt
          fi

      - name: 🔧 Configurer variables d'environnement
        run: |
          echo "DATABASE_URL=mysql://root:root@127.0.0.1:3306/library_test" >> $GITHUB_ENV
          echo "DB_HOST=127.0.0.1" >> $GITHUB_ENV
          echo "DB_PORT=3306" >> $GITHUB_ENV
          echo "DB_NAME=library_test" >> $GITHUB_ENV
          echo "DB_USER=root" >> $GITHUB_ENV
          echo "DB_PASSWORD=root" >> $GITHUB_ENV
          echo "SECRET_KEY=test-secret-key" >> $GITHUB_ENV

      - name: ⏳ Attendre MySQL
        run: |
          for i in {1..30}; do
            mysqladmin --protocol=TCP ping -h127.0.0.1 -uroot -proot 2>/dev/null && break
            sleep 2
          done

      - name: 🧪 Lancer les tests avec coverage
        working-directory: ./backend/${{ matrix.service }}
        run: |
          TEST_FILES=$(find . -name "test_*.py" -o -path "*/tests/test_*.py" 2>/dev/null | wc -l)
          if [ "$TEST_FILES" -gt 0 ]; then
            pytest --disable-warnings --cov=. --cov-report=xml:coverage.xml || TEST_EXIT_CODE=$?
          else
            echo '<?xml version="1.0"?><coverage version="7.0"><sources><source>.</source></sources><packages/></coverage>' > coverage.xml
          fi
          exit ${TEST_EXIT_CODE:-0}

      - name: 📤 Upload coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ matrix.service }}
          path: backend/${{ matrix.service }}/coverage.xml

  combine-coverage:
    name: 📊 Combinaison des Rapports de Couverture
    runs-on: ubuntu-latest
    needs: tests-unitaires
    steps:
      - uses: actions/checkout@v4

      - name: 📥 Télécharger tous les coverage
        uses: actions/download-artifact@v4
        with:
          path: coverage-reports
          pattern: coverage-*

      - name: 📤 Upload combiné
        uses: actions/upload-artifact@v4
        with:
          name: all-coverage-reports
          path: coverage-reports/

  sonar-analysis:
    name: 🔍 Analyse SonarQube
    runs-on: ubuntu-latest
    needs: combine-coverage
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 📥 Télécharger coverage pour Sonar
        uses: actions/download-artifact@v4
        with:
          name: all-coverage-reports
          path: sonar-coverage

      - name: 🔧 Corriger <source> dans coverage.xml
        run: |
          mkdir -p sonar-coverage-fixed
          for xml in sonar-coverage/*.xml; do
            svc=$(basename "$xml" | sed -E 's/^coverage-//; s/\.xml$//')
            if [ -d "backend/${svc}" ]; then
              sed "s|<source>.</source>|<source>backend/${svc}</source>|g" "$xml" > "sonar-coverage-fixed/${svc}.xml"
            else
              cp "$xml" "sonar-coverage-fixed/${svc}.xml"
            fi
          done

      - name: 🧠 SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@v5.0.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.organization=library-system-management
            -Dsonar.projectKey=AymenZahed_Library-management-system
            -Dsonar.sources=backend
            -Dsonar.python.coverage.reportPaths=sonar-coverage-fixed/*.xml
            -Dsonar.exclusions=**/tests/**,**/migrations/**,**/__pycache__/**,**/venv/**
            -Dsonar.test.exclusions=**/tests/**
            -Dsonar.coverage.exclusions=**/tests/**,**/migrations/**
            -Dsonar.qualitygate.wait=true
            -Dsonar.qualitygate.timeout=300

      - name: ⚠️ Quality Gate Failed
        if: failure()
        run: |
          echo ""
          echo "╔══════════════════════════════════════════════════════════╗"
          echo "║          ⚠️  QUALITY GATE FAILED                        ║"
          echo "╚══════════════════════════════════════════════════════════╝"
          echo ""
          echo "❌ Le code n'a pas passé les critères de qualité SonarQube"
          echo ""
          echo "📊 Problèmes possibles:"
          echo "   • Code coverage trop faible"
          echo "   • Bugs ou vulnerabilités détectés"
          echo "   • Code smells ou duplications"
          echo "   • Problèmes de sécurité"
          echo ""
          echo "🔗 Consultez le rapport détaillé sur SonarCloud:"
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            echo "   https://sonarcloud.io/project/pull_requests_list?id=MMA-PROJECT-V2_Library-Management-System"
          else
            echo "   https://sonarcloud.io/dashboard?id=AymenZahed_Library-management-system"
          fi
          echo ""
          echo "💡 Actions recommandées:"
          echo "   1. Vérifier les issues sur SonarCloud"
          echo "   2. Corriger les bugs et vulnérabilités critiques"
          echo "   3. Améliorer la couverture de tests"
          echo "   4. Refactoriser le code avec des code smells"
          echo ""

          # Ne pas faire échouer le pipeline en développement
          if [[ "${{ github.ref }}" == "refs/heads/develop" ]]; then
            echo "⚠️  Mode développement: le pipeline continue malgré l'échec du Quality Gate"
            exit 0
          else
            echo "❌ Le pipeline est interrompu (branche: ${{ github.ref_name }})"
            exit 1
          fi

      - name: 📊 Résumé SonarQube
        if: success()
        run: |
          echo ""
          echo "╔══════════════════════════════════════════════════════════╗"
          echo "║          ✅ ANALYSE SONARQUBE RÉUSSIE                   ║"
          echo "╚══════════════════════════════════════════════════════════╝"
          echo "📊 Métriques analysées:"
          echo "   • Couverture de code (tous services)"
          echo "   • Qualité et sécurité du code"
          echo "   • Code smells et duplications"
          echo "   • Quality Gate: PASSED ✅"
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            echo "   • Décoration de la PR #${{ github.event.pull_request.number }}"
          fi

  sonar-info:
    name: ℹ️ Info SonarQube (Push non-main)
    runs-on: ubuntu-latest
    needs: tests-unitaires
    if: github.event_name == 'push' && github.ref != 'refs/heads/main'

    steps:
      - name: ℹ️ Information Plan Gratuit
        run: |
          echo ""
          echo "╔══════════════════════════════════════════════════════════╗"
          echo "║     ℹ️  ANALYSE SONARQUBE - PUSH SUR ${{ github.ref_name }}"
          echo "╚══════════════════════════════════════════════════════════╝"
          echo ""
          echo "📌 Branche: ${{ github.ref_name }}"
          echo "✅ Tests unitaires: RÉUSSIS"
          echo "💡 Pour analyse SonarQube: créer une PR vers main"

  pipeline-summary:
    name: 📋 Résumé du Pipeline
    runs-on: ubuntu-latest
    needs: [initiation, discover-services, tests-unitaires, sonar-analysis]
    if: always()

    steps:
      - name: 📊 Statut du Pipeline
        run: |
          echo ""
          echo "╔══════════════════════════════════════════════════════════╗"
          echo "║          📊 RÉSUMÉ DU PIPELINE CI/CD                    ║"
          echo "╚══════════════════════════════════════════════════════════╝"
          echo ""
          echo "📅 Date: $(date)"
          echo "🌿 Branche: ${{ github.ref_name }}"
          echo "👤 Auteur: ${{ github.actor }}"
          echo "🗄️  Base de données: MySQL 8.0"
          echo "📋 Event: ${{ github.event_name }}"
          echo ""
          echo "✅ Statut des jobs:"
          echo "   1️⃣  Initiation: ${{ needs.initiation.result }}"
          echo "   2️⃣  Découverte services: ${{ needs.discover-services.result }}"
          echo "   3️⃣  Tests unitaires: ${{ needs.tests-unitaires.result }}"

          # Afficher le statut SonarQube si exécuté
          if [[ "${{ needs.sonar-analysis.result }}" != "" ]]; then
            echo "   4️⃣  SonarQube: ${{ needs.sonar-analysis.result }}"
          fi

          echo ""

          # Vérifier si les tests ont réussi
          if [[ "${{ needs.tests-unitaires.result }}" == "success" ]]; then
            echo "🎉 TESTS RÉUSSIS! Tous les services passent les tests unitaires."
            
            # Avertissement si SonarQube a échoué
            if [[ "${{ needs.sonar-analysis.result }}" == "failure" ]]; then
              echo ""
              echo "⚠️  ATTENTION: Quality Gate SonarQube FAILED"
              echo "   📊 Consultez https://sonarcloud.io pour les détails"
              echo "   💡 Corrigez les problèmes de qualité avant le merge"
              
              # Fail seulement sur main
              if [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
                echo ""
                echo "❌ Pipeline échoué à cause du Quality Gate (branche main)"
                exit 1
              else
                echo ""
                echo "✅ Pipeline continue (branche de développement)"
              fi
            fi
            
            exit 0
          else
            echo "❌ PIPELINE ÉCHOUÉ! Vérifiez les logs des tests."
            exit 1
          fi
