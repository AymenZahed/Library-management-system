# ✅ SonarQube Project Configuration

sonar.projectKey=your.actual.projectKey        # <-- replace with real key from SonarCloud / SonarQube
sonar.organization=your-org-if-using-sonarcloud # <-- remove or replace if using SonarQube server
sonar.projectName=Library Management System
sonar.projectVersion=1.0

# 👇 Source and test paths
sonar.sources=backend
# tests are under backend/*/tests — point sonar to them or omit if you don't want test files analyzed
sonar.tests=backend
sonar.sourceEncoding=UTF-8

# 👇 Coverage configuration
sonar.python.coverage.reportPaths=coverage-reports/coverage.xml
sonar.coverage.exclusions=**/tests/**,**/migrations/**

# 👇 Wait for quality gate result
sonar.qualitygate.wait=true
